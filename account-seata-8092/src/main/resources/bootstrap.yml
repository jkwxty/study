spring:
  application:
    name: account-seata-server
  cloud:
    nacos:
      server-addr: 139.159.198.7:8849
      username: nacos
      password: nacos
      discovery:
        namespace: public
      config:
        #命名空间
        namespace: dev
        group: dev
        #配置文件前缀
        prefix: data-source-local
        #配置文件后缀
        file-extension: yaml
